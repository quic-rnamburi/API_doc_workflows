---
title: "vocoder_cmn_api.h"
description: "This file contains vocoder modules' commmon APIs."
---

## Structures

### `param_id_vocoder_enable_dtx_mode_t`

@h2xmlp_parameter {"PARAM_ID_VOCODER_ENABLE_DTX_MODE", PARAM_ID_VOCODER_ENABLE_DTX_MODE} @h2xmlp_description {Enables/Disables the Discontinous Transmission Mode} @h2xmlp_toolPolicy {Calibration}

**Members**

<ParamField path="enable" type="uint32_t">
  @h2xmle_description {Enables the DTX transmission mode} @h2xmle_rangeList {Disabled=0;Enabled=1} @h2xmle_default {0}
</ParamField>

### `param_id_vocoder_minmax_rate_param_t`

@h2xmlp_parameter {"PARAM_ID_VOCODER_MINMAX_RATE", PARAM_ID_VOCODER_MINMAX_RATE} @h2xmlp_description {Parameter for setting the min and max rates on the encoder module} @h2xmlp_toolPolicy {Calibration}

**Members**

<ParamField path="min_rate" type="uint32_t">
  @h2xmle_description {Minimum supported encoding rate} @h2xmle_rangeList {"SILENCE_RATE"=0,"QUARTER_RATE"=1,"HALF_RATE"=3,"FULL_RATE"=4 } @h2xmle_policy {Basic}
</ParamField>
<ParamField path="max_rate" type="uint32_t">
  @h2xmle_description {Maximum supported encoding rate} @h2xmle_rangeList {"SILENCE_RATE"=0,"QUARTER_RATE"=1,"HALF_RATE"=3,"FULL_RATE"=4 } @h2xmle_policy {Basic}
</ParamField>

### `param_id_vocoder_enc_rate_param_t`

@h2xmlp_parameter {"PARAM_ID_VOCODER_ENC_RATE", PARAM_ID_VOCODER_ENC_RATE} @h2xmlp_description {Parameter for setting encoder rate} @h2xmlp_toolPolicy {Calibration}

**Members**

<ParamField path="rate" type="uint32_t">
  @h2xmle_description {Encoding rate to be set.
</ParamField>

### `param_id_vocoder_rate_modulation_param_t`

@h2xmlp_parameter {"PARAM_ID_VOCODER_RATE_MODULATION", PARAM_ID_VOCODER_RATE_MODULATION} @h2xmlp_description {Parameter for setting the encoding rate for modulation The exact interpretation of the value and valid range depends on the type of encoder} @h2xmlp_toolPolicy {Calibration}

**Members**

<ParamField path="rate_modulation_param" type="uint32_t">
  @h2xmle_description {Encoder rate modulation command} @h2xmle_policy {Basic}
</ParamField>

### `param_id_tty_mode_t`

@h2xmlp_parameter {"PARAM_ID_TTY_MODE", PARAM_ID_TTY_MODE} @h2xmlp_description {Parameter for setting the TTY mode of operation} @h2xmlp_toolPolicy {Calibration}

**Members**

<ParamField path="mode" type="uint32_t">
  @h2xmle_description {TTY mode to be set} @h2xmle_rangeList {"VOICE_TTY_MODE_OFF"=0, "VOICE_TTY_MODE_HCO"=1, "VOICE_TTY_MODE_VCO"=2, "VOICE_TTY_MODE_FULL"=3} @h2xmle_policy {Basic}
</ParamField>

### `event_id_vocoder_operating_mode_t`

@h2xmlp_parameter {"EVENT_ID_VOCODER_OPERATING_MODE", EVENT_ID_VOCODER_OPERATING_MODE} @h2xmlp_description { Operating mode that is returned by the vocoder.

**Members**

<ParamField path="operating_mode" type="uint32_t">
  @h2xmle_description {Operating mode returned by the vocoder} @h2xmle_rangeList {"VOCODER_OPERATING_MODE_NB"=0, "VOCODER_OPERATING_MODE_WB"=1, "VOCODER_OPERATING_MODE_SWB"=2, "VOCODER_OPERATING_MODE_FB"=3} @h2xmle_policy {Basic}
</ParamField>
<ParamField path="client_data" type="uint32_t">
  @h2xmle_description {Client data to be used by the modem} @h2xmle_policy {Basic}
</ParamField>

## Type Definitions

### `vocoder_client_id_t`

```c
typedef enum vocoder_client_id_t vocoder_client_id_t
```

### `tty_cdma_vocoder_type_t`

```c
typedef enum tty_cdma_vocoder_type_t tty_cdma_vocoder_type_t
```

### `param_id_vocoder_enable_dtx_mode_t`

```c
typedef struct param_id_vocoder_enable_dtx_mode_t param_id_vocoder_enable_dtx_mode_t
```

### `param_id_vocoder_minmax_rate_param_t`

```c
typedef struct param_id_vocoder_minmax_rate_param_t param_id_vocoder_minmax_rate_param_t
```

### `param_id_vocoder_enc_rate_param_t`

```c
typedef struct param_id_vocoder_enc_rate_param_t param_id_vocoder_enc_rate_param_t
```

### `param_id_vocoder_rate_modulation_param_t`

```c
typedef struct param_id_vocoder_rate_modulation_param_t param_id_vocoder_rate_modulation_param_t
```

### `param_id_tty_mode_t`

```c
typedef struct param_id_tty_mode_t param_id_tty_mode_t
```

### `event_id_vocoder_operating_mode_t`

```c
typedef struct event_id_vocoder_operating_mode_t event_id_vocoder_operating_mode_t
```

## Enumerations

### `vocoder_client_id_t`

#### Values

| Name | Value | Description |
|------|-------|-------------|
| `AUDIO_CLIENT` | = 0x0 |  |
| `VOICE_CLIENT` | = 0x1 |  |

### `tty_cdma_vocoder_type_t`

#### Values

| Name | Value | Description |
|------|-------|-------------|
| `TTY_CDMA_V13K` | = 0 |  |
| `TTY_CDMA_EVRC` |  |  |
| `TTY_CDMA_FOURGV` |  |  |

## Macros

### `VOICE_TTY_MODE_OFF`

@h2xml_title1 {Vocoder Common API} @h2xml_title_agile_rev {Vocoder Common API} @h2xml_title_date {April, 2019}

```c
#define VOICE_TTY_MODE_OFF 0 /* TTY OFF or disabled */
```

### `VOICE_TTY_MODE_HCO`

```c
#define VOICE_TTY_MODE_HCO 1 /* Hearing carry over mode. */
```

### `VOICE_TTY_MODE_VCO`

```c
#define VOICE_TTY_MODE_VCO 2 /* Voice carry over mode. */
```

### `VOICE_TTY_MODE_FULL`

```c
#define VOICE_TTY_MODE_FULL 3 /* FULL mode. */
```

### `VOCODER_OPERATING_MODE_NB`

```c
#define VOCODER_OPERATING_MODE_NB 0 /* Vocoder bandwidth mode is 8 kHz. */
```

### `VOCODER_OPERATING_MODE_WB`

```c
#define VOCODER_OPERATING_MODE_WB 1 /* Vocoder bandwidth mode is 16 kHz. */
```

### `VOCODER_OPERATING_MODE_SWB`

```c
#define VOCODER_OPERATING_MODE_SWB 2 /* Vocoder bandwidth mode is 32 kHz. */
```

### `VOCODER_OPERATING_MODE_FB`

```c
#define VOCODER_OPERATING_MODE_FB 3 /* Vocoder bandwidth mode is 48 kHz. */
```

### `VOCODER_OPERATING_MODE_NONE`

```c
#define VOCODER_OPERATING_MODE_NONE AR_NON_GUID(0xffffffff)
```

### `PARAM_ID_VOCODER_ENABLE_DTX_MODE`

```c
#define PARAM_ID_VOCODER_ENABLE_DTX_MODE 0x08001123
```

### `PARAM_ID_VOCODER_MINMAX_RATE`

ID of the parameter used to configure the encoder minimum and maximum rates.

```c
#define PARAM_ID_VOCODER_MINMAX_RATE 0x08001124
```

### `PARAM_ID_VOCODER_ENC_RATE`

ID of the parameter used to configure the encoding rate.

```c
#define PARAM_ID_VOCODER_ENC_RATE 0x080011B1
```

### `PARAM_ID_VOCODER_RATE_MODULATION`

ID of the parameter used to configure the encoding rate for modulation.

```c
#define PARAM_ID_VOCODER_RATE_MODULATION 0x08001125
```

### `PARAM_ID_TTY_MODE`

ID of the parameter used to set the TTY mode.

```c
#define PARAM_ID_TTY_MODE 0x08001127
```

### `PARAM_ID_VOCODER_RESET`

Parameter to reset voice encoder/decoder.

```c
#define PARAM_ID_VOCODER_RESET 0x08001128
```

### `EVENT_ID_VOCODER_OPERATING_MODE`

Event ID that is raised by the vocoder when client registers for operating mode updates with the vocoder.

```c
#define EVENT_ID_VOCODER_OPERATING_MODE 0x08001197
```
