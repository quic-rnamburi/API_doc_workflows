---
title: "audioss_dsp_mem_api.h"
description: "This file contains Audioss DSP memory util definitions."
---

## Structures

### `audioss_dsp_mem_request_t`

**Members**

<ParamField path="mem_type" type="uint32_t">
  type of memory, from where client is requesting memory Currently on DRAM memory type(1) is supported.
</ParamField>
<ParamField path="size_in_bytes" type="uint32_t">
  size of memory required in bytes
</ParamField>

### `audioss_dsp_mem_request_rsp_t`

**Members**

<ParamField path="size_in_bytes" type="uint32_t">
  size of memory to be released in bytes
</ParamField>
<ParamField path="phy_addr_lsw" type="uint32_t">
</ParamField>
<ParamField path="phy_addr_msw" type="uint32_t">
  least significant word of the adress
</ParamField>

### `audioss_dsp_mem_release_t`

**Members**

<ParamField path="mem_type" type="uint32_t">
  type of memory, from where client is requesting memory Currently on DRAM memory type(1) is supported.
</ParamField>
<ParamField path="size_in_bytes" type="uint32_t">
  size of memory to be released in bytes
</ParamField>
<ParamField path="phy_addr_lsw" type="uint32_t">
</ParamField>
<ParamField path="phy_addr_msw" type="uint32_t">
  least significant word of the adress
</ParamField>

## Type Definitions

### `audioss_dsp_mem_request_t`

```c
typedef struct audioss_dsp_mem_request_t audioss_dsp_mem_request_t
```

### `audioss_dsp_mem_request_rsp_t`

```c
typedef struct audioss_dsp_mem_request_rsp_t audioss_dsp_mem_request_rsp_t
```

### `audioss_dsp_mem_release_t`

```c
typedef struct audioss_dsp_mem_release_t audioss_dsp_mem_release_t
```

## Macros

### `PARAM_ID_RSC_AUDIOSS_DSP_MEM`

This param is used to allocate or free the DSP memory.

```c
#define PARAM_ID_RSC_AUDIOSS_DSP_MEM 0x0800151E
```

### `AUDIOSS_DRAM_MEMORY`

The macro AUDIOSS_DRAM_MEMORY can be used to mention the type of memory that is to be allocated or freed.

```c
#define AUDIOSS_DRAM_MEMORY 1
```
